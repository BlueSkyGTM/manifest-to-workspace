# CONTEXT.md — vault/
This folder starts EMPTY and is correct when empty. It fills only as material EARNS its place by
clearing a deferral point. Do not populate it in the skeleton pass.

Role: INGEST holding. Raw excavated material lands here, addressed, untouched. Nothing is loaded into
context from here speculatively (M2W). Filled by stage 01 (excavation).
